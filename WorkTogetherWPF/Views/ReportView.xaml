<UserControl x:Class="WorkTogetherWPF.Views.ReportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WorkTogetherWPF.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Background="White">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="1000"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <DataGrid ItemsSource="{Binding Reports}" SelectedItem="{Binding SelectedReport}" AutoGenerateColumns="False" Grid.Column="1"  IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Sujet" Binding="{Binding Subject}" Width="*" />
                <DataGridTextColumn Header="Corps" Binding="{Binding Object}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>
        <Button x:Name="Supprimer" Click="Delete_Click" Content="Supprimer" Grid.Column="2" Visibility="{Binding Path=User, Source={x:Static Application.Current}, Converter={StaticResource SharedUserRoleToVisibilityConverter}, ConverterParameter='ROLE_ADMIN'}"/>
    </Grid>
</UserControl>
